{
  "$schema": "https://json.schemastore.org/claude-code-plugin-manifest.json",
  "name": "claude-journal",
  "description": "Per-device Stop/SessionStart hooks that push session breadcrumbs to your own encrypted claude-journal data repo, plus a single /journal command: `setup` bootstraps the data repo, `schedule` creates the nightly consolidator routine, `consolidate` distills recent breadcrumbs on demand (including the current session), and `accept`/`skip`/`edit` resolve consolidation proposals.",
  "version": "0.6.0",
  "author": {
    "name": "Anthony Skenter"
  },
  "homepage": "https://github.com/askenter/claude-journal-tools",
  "repository": "https://github.com/askenter/claude-journal-tools",
  "license": "MIT",
  "keywords": ["journal", "memory", "consolidation", "hooks", "breadcrumbs"]
}
